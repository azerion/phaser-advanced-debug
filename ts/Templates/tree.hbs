{{listItemOpen}}
    {{typeString}}

    {{#if name}}
        <span class="weak">({{name}}) {{#if children}}({{children.length}}){{/if}}</span>
    {{/if}}

    {{#if children}}
        <ul>
            {{#each children}}
                {{> sceneTree}}
            {{/each}}
        </ul>
    {{/if}}
</li>
